{
  "schema_version": "cigar.qualification-gaps.v1",
  "generated_for": "development-workspace",
  "gaps": [
    {
      "id": "unborn-source-revision",
      "release_blocking": true,
      "owner": "release",
      "condition": "The workspace has no committed Git revision.",
      "closure": "Commit the candidate, rebuild only from the signed source archive, and bind every report to that revision."
    },
    {
      "id": "external-platform-builds",
      "release_blocking": true,
      "owner": "release-ci",
      "condition": "The five claimed GNU Linux, Windows, and macOS binary targets and the multi-architecture OCI bytes have not all been built and tested here.",
      "closure": "Run the artifact matrix on isolated native builders and attach installed-byte qualification evidence."
    },
    {
      "id": "production-signing-not-available",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "No production signing identity or trusted public-root bundle is available in this workspace.",
      "closure": "Sign artifacts and evidence through the approved isolated signer and verify the envelopes offline."
    },
    {
      "id": "third-party-license-review",
      "release_blocking": true,
      "owner": "legal-release",
      "condition": "Some locked optional or platform dependencies may lack locally available license metadata and final native/image content has not been reconciled.",
      "closure": "Run the complete inventory in every final builder, resolve every review-required expression, reconcile final artifact/native/image contents, and approve the generated notices."
    },
    {
      "id": "rust-sdk-dependency-publication",
      "release_blocking": true,
      "owner": "sdk",
      "condition": "The two reviewed security support packages, 16 CIGAR dependency crates, and SDK pass topological cargo package plus a clean default-feature consumer against an offline local registry with exact Ring-backed fork identities. The workspace still has no committed candidate, and none of the 19 exact package versions has an approved crates.io ownership or publication receipt.",
      "closure": "Commit the exact candidate and rerun the final VCS-bound package contracts; have an approved registry owner publish the approved package chain in the documented order and capture every registry identity/checksum receipt; then pass a clean crates.io consumer on the supported matrix and reconcile signing, SBOM, license, provenance, and artifact evidence."
    },
    {
      "id": "platform-installers-not-defined",
      "release_blocking": true,
      "owner": "distribution",
      "condition": "No Homebrew, WinGet/MSI, deb/rpm, or equivalent installer is yet claimed in the artifact matrix.",
      "closure": "Choose the supported installer/formula set, add exact allowlisted artifacts, and pass clean non-admin install, upgrade, offline smoke, and uninstall tests for each."
    },
    {
      "id": "publishing-and-notarization",
      "release_blocking": true,
      "owner": "release",
      "condition": "Registry publishing, Apple notarization, Windows signing, and image transparency-log publication are external operations.",
      "closure": "Qualify the exact signed bytes, publish without rebuild, and attach registry/notarization receipts."
    },
    {
      "id": "final-artifact-security-qualification",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "The exact final native, ecosystem, plugin, installer, and OCI bytes do not yet have approved vulnerability, malware-indicator, secret, and unexpected-endpoint scan receipts.",
      "closure": "Run pinned approved scanners against every final packed and unpacked artifact, bind passing reports to artifact and source digests, and attach security:final-artifact-scan evidence."
    },
    {
      "id": "live-operation-exercises",
      "release_blocking": true,
      "owner": "operations",
      "condition": "All eight required recovery and maintenance runbooks have passed deterministic static validation, but they have not been executed against exact installed candidate bytes in an isolated live environment.",
      "closure": "Run the eight explicit live drivers in the enforced operation sandbox, bind their raw receipts to the committed candidate revision and complete artifact set, and attach the signed operation summary."
    },
    {
      "id": "installed-documentation-execution",
      "release_blocking": true,
      "owner": "documentation-release",
      "condition": "Local documentation generation, links, anchors, and commands pass, but installed-candidate and live command blocks cannot execute until exact candidate artifacts and operation drivers exist.",
      "closure": "Execute every installed-candidate and live command manifest entry against the final distributed bytes with declared variables, require zero failed commands, and attach the raw docs report."
    },
    {
      "id": "final-artifact-sbom-reconciliation",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "The deterministic local SBOM covers locked language components and binds supplied artifact bytes, but final native libraries, extension modules, installer contents, plugin executables, and OCI image layers are not all available for reconciliation.",
      "closure": "Inventory the unpacked final artifact set and OCI layers on release builders, merge native and packaged components into SPDX and CycloneDX, require reviewed licenses, and prove the SBOM artifact binding matches every distributed byte."
    }
  ]
}
