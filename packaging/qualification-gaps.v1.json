{
  "schema_version": "cigar.qualification-gaps.v1",
  "generated_for": "development-and-initial-beta-pre-freeze",
  "gaps": [
    {
      "id": "dirty-unbound-source-revision",
      "release_blocking": true,
      "owner": "release",
      "condition": "The latest observed committed development baseline is b26e32439bcf5bf42f0d47d993b935cb3eb5f182 (tree 4db649dc926e1550637c6817a4b20e4fd4ca5a2c), but the worktree contains active implementation changes and has not been frozen as either a GA or beta candidate. Receipts for source 4081a8355b8e6bd5959dcc44c48b63b9d8dc55ca are failed or incomplete historical diagnostics, are not candidate-bound, and are nonqualifying.",
      "closure": "Reconcile the intentional source, corpus, policy, and status changes; commit the exact candidate; prove the worktree remains clean before and after smoke qualification; rebuild from its source archive; and regenerate every report with matching commit, tree, source-archive, policy, toolchain, and artifact digests."
    },
    {
      "id": "mutation-campaign-not-executed",
      "release_blocking": true,
      "owner": "quality",
      "condition": "The native Apple-silicon mutation-only xtask route and independent raw-outcome verifier are implemented, but the required clean-candidate four-hour cargo-mutants 27.1.0 campaign has not been executed. No current receipt therefore proves the 90% score, complete 24-package production scope, zero timeouts, and zero viable critical survivors.",
      "closure": "Freeze a clean committed macOS-arm64 candidate and run cargo xtask test mutations --verify in a fresh external evidence workspace for at least 14,400 observed seconds. Investigate every survivor, timeout, or baseline failure; after any source fix create a new candidate and rerun until the independently reopened source-bound receipt passes all mutation gates."
    },
    {
      "id": "external-platform-builds",
      "release_blocking": true,
      "owner": "release-ci",
      "condition": "The five claimed GNU Linux, Windows, and macOS binary targets and the multi-architecture OCI bytes have not all been built and tested here.",
      "closure": "Run the artifact matrix on isolated native builders and attach installed-byte qualification evidence."
    },
    {
      "id": "production-signing-not-available",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "No production signing identity or trusted public-root bundle is available in this workspace.",
      "closure": "Sign artifacts and evidence through the approved isolated signer and verify the envelopes offline."
    },
    {
      "id": "third-party-license-review",
      "release_blocking": true,
      "owner": "legal-release",
      "condition": "Some locked optional or platform dependencies may lack locally available license metadata and final native/image content has not been reconciled.",
      "closure": "Run the complete inventory in every final builder, resolve every review-required expression, reconcile final artifact/native/image contents, and approve the generated notices."
    },
    {
      "id": "rust-sdk-dependency-publication",
      "release_blocking": true,
      "owner": "sdk",
      "condition": "The two reviewed security support packages, 16 CIGAR dependency crates, and SDK pass topological cargo package plus a clean default-feature consumer against an offline local registry with exact Ring-backed fork identities. That receipt is not bound to a clean release-candidate commit, and none of the 19 exact package versions has an approved crates.io ownership or publication receipt.",
      "closure": "Create the exact clean candidate and rerun the final VCS-bound package contracts; have an approved registry owner publish the approved package chain in the documented order and capture every registry identity/checksum receipt; then pass a clean crates.io consumer on the supported matrix and reconcile signing, SBOM, license, provenance, and artifact evidence."
    },
    {
      "id": "platform-installers-not-defined",
      "release_blocking": true,
      "owner": "distribution",
      "condition": "No Homebrew, WinGet/MSI, deb/rpm, or equivalent installer is yet claimed in the artifact matrix.",
      "closure": "Choose the supported installer/formula set, add exact allowlisted artifacts, and pass clean non-admin install, upgrade, offline smoke, and uninstall tests for each."
    },
    {
      "id": "publishing-and-notarization",
      "release_blocking": true,
      "owner": "release",
      "condition": "Registry publishing, Apple notarization, Windows signing, and image transparency-log publication are external operations.",
      "closure": "Qualify the exact signed bytes, publish without rebuild, and attach registry/notarization receipts."
    },
    {
      "id": "final-artifact-security-qualification",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "The exact final native, ecosystem, plugin, installer, and OCI bytes do not yet have approved vulnerability, malware-indicator, secret, and unexpected-endpoint scan receipts.",
      "closure": "Run pinned approved scanners against every final packed and unpacked artifact, bind passing reports to artifact and source digests, and attach security:final-artifact-scan evidence."
    },
    {
      "id": "live-operation-exercises",
      "release_blocking": true,
      "owner": "operations",
      "condition": "All eight required recovery and maintenance runbooks have passed deterministic static validation, but they have not been executed against exact installed candidate bytes in an isolated live environment.",
      "closure": "Run the eight explicit live drivers in the enforced operation sandbox, bind their raw receipts to the committed candidate revision and complete artifact set, and attach the signed operation summary."
    },
    {
      "id": "installed-documentation-execution",
      "release_blocking": true,
      "owner": "documentation-release",
      "condition": "Local documentation generation, links, anchors, and commands pass, but installed-candidate and live command blocks cannot execute until exact candidate artifacts and operation drivers exist.",
      "closure": "Execute every installed-candidate and live command manifest entry against the final distributed bytes with declared variables, require zero failed commands, and attach the raw docs report."
    },
    {
      "id": "final-artifact-sbom-reconciliation",
      "release_blocking": true,
      "owner": "release-security",
      "condition": "The deterministic local SBOM covers locked language components and binds supplied artifact bytes, but final native libraries, extension modules, installer contents, plugin executables, and OCI image layers are not all available for reconciliation.",
      "closure": "Inventory the unpacked final artifact set and OCI layers on release builders, merge native and packaged components into SPDX and CycloneDX, require reviewed licenses, and prove the SBOM artifact binding matches every distributed byte."
    },
    {
      "id": "initial-beta-contract-and-state-safety",
      "release_blocking": true,
      "owner": "beta-runtime",
      "condition": "The administration-only capability surface, exact command aliases, and descriptor-relative state locking were reconciled and regression-tested in committed development source. The remaining release blocker is a proven production path that assembles and independently verifies complete candidate, source-archive, artifact, version, profile, purpose, trust-policy, receipt-status, and complete-set bindings.",
      "closure": "Complete the signed beta release-evidence assembly and independent offline verifier; cover missing, duplicate, stale, wrong-purpose, wrong-trust, and source/artifact substitution cases; then rerun the runtime and evidence suites against the exact frozen candidate."
    },
    {
      "id": "initial-beta-clean-candidate-and-linux-builder",
      "release_blocking": true,
      "owner": "beta-release-ci",
      "condition": "No clean committed 0.1.0-beta.1 candidate, deterministic source archive, canonical source descriptor, or qualified native Ubuntu 24.04 x86_64/glibc 2.39 builder receipt exists. Development-workspace profile, feature-isolation, and CLI test passes are not candidate-bound release evidence.",
      "closure": "Commit the exact beta source and prove a clean read-only checkout before and after gates; bind commit, tree, SOURCE_DATE_EPOCH, source-archive digest, policies, contracts, tools, builder, and network mode; then rerun all beta checks and build through a qualified isolated native Ubuntu 24.04 x86_64/glibc 2.39 builder with Rust 1.92.0 and Python 3.14.6, preferably with a second independent builder for reproducibility."
    },
    {
      "id": "initial-beta-six-artifact-qualification",
      "release_blocking": true,
      "owner": "beta-distribution",
      "condition": "The closed beta set of source, docs, schemas, conformance, licenses, and cigar-linux-x86_64-gnu archives has not been assembled from one candidate, contract-verified, or independently rebuilt; no complete beta archive assembler or independent offline verifier has qualified the set.",
      "closure": "Produce exactly the six filenames pinned in packaging/beta/artifact-matrix.v1.json; require the binary archive to contain only bin/cigar; reject every missing/extra/unsafe member and metadata violation; generate canonical checksums; prove deterministic independent rebuilds; and verify the complete set offline against pinned contracts."
    },
    {
      "id": "initial-beta-installed-offline-and-security",
      "release_blocking": true,
      "owner": "beta-security",
      "condition": "No exact beta binary archive has passed unprivileged clean-host installation, OS-enforced no-egress execution, full included-command and excluded-surface smoke, state concurrency/durability tests, or approved packed-and-unpacked final-byte vulnerability, malware-indicator, secret, endpoint, and developer-path scans.",
      "closure": "Install the exact archive on clean Ubuntu 24.04 x86_64/glibc 2.39 without source, compiler, privilege, or undeclared runtime packages; enforce network denial; pass all positive, restart, persistence, permission, concurrency, cancellation, malformed-state, and negative excluded-command cases; then bind zero-critical/high and no-unknown/skipped final-byte scan receipts to the source and artifact digests."
    },
    {
      "id": "initial-beta-sbom-provenance-and-signing",
      "release_blocking": true,
      "owner": "beta-release-security",
      "condition": "The beta has no approved final-content license reconciliation, artifact-bound SPDX and CycloneDX SBOMs, provenance for all six artifacts, isolated production beta signing identity, or independent trusted beta root bundle. Development identities and GA signature purposes do not qualify.",
      "closure": "Reconcile exact source and binary contents with approved notices; generate complete artifact-bound SPDX/CycloneDX and provenance binding source, materials, builders, tools, parameters, network mode, and reproducibility; sign every qualification receipt and attachment using cigar-beta-qualification-evidence-v1 and sign artifacts, checksums, SBOMs, provenance, and release evidence using only their reserved cigar-beta-release-* purposes through the isolated signer; then verify every envelope offline against independent approved roots."
    },
    {
      "id": "initial-beta-publication-and-readback",
      "release_blocking": true,
      "owner": "beta-release",
      "condition": "No complete signed cigar.beta.release-evidence.v1 set, independent offline-verifier pass, release-owner approval, protected prerelease publisher authority, exact-byte publication, or public digest readback exists for v0.1.0-beta.1.",
      "closure": "Assemble the closed beta evidence set with no failed, skipped, waived, unknown, stale, dirty, or unbound result; pass independent offline verification; obtain publisher approval; publish the already-qualified exact bytes without rebuilding; read every public object back by digest; and prove the prerelease did not update latest, stable, or GA channels."
    }
  ]
}
