# @generated by sdk/generate_clients.py; do not edit.
from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class ErrorDefinition:
    numeric_code: int
    http_status: int
    retry: str
    message: str
    remediation: str


ERROR_CATALOG: dict[str, ErrorDefinition] = {
    'INVALID_ARGUMENT': ErrorDefinition(numeric_code=1000, http_status=400, retry='never', message='request argument is invalid', remediation='correct the request using the published schema'),
    'LIMIT_EXCEEDED': ErrorDefinition(numeric_code=1001, http_status=400, retry='never', message='a published resource limit was exceeded', remediation='reduce the bounded request size'),
    'UNSUPPORTED_SCHEMA': ErrorDefinition(numeric_code=1002, http_status=400, retry='never', message='schema version is unsupported', remediation='use a supported protocol schema'),
    'UNKNOWN_PRINCIPAL': ErrorDefinition(numeric_code=1100, http_status=401, retry='after_reauthorization', message='principal is not authenticated', remediation='authenticate using a configured identity provider'),
    'INVALID_CAPABILITY': ErrorDefinition(numeric_code=1101, http_status=403, retry='after_reauthorization', message='capability does not authorize this operation', remediation='request a valid attenuated capability'),
    'CAPABILITY_EXPIRED': ErrorDefinition(numeric_code=1102, http_status=403, retry='after_reauthorization', message='capability has expired', remediation='request a current attenuated capability'),
    'SOURCE_UNAVAILABLE': ErrorDefinition(numeric_code=1200, http_status=503, retry='after_backoff', message='source is temporarily unavailable', remediation='retry after source recovery'),
    'SNAPSHOT_INCOMPLETE': ErrorDefinition(numeric_code=1201, http_status=503, retry='after_backoff', message='source snapshot is incomplete', remediation='repeat ingestion after the source is stable'),
    'INTEGRITY_FAILURE': ErrorDefinition(numeric_code=1202, http_status=500, retry='never', message='integrity verification failed', remediation='quarantine the record and inspect correlation logs'),
    'INDEX_STALE': ErrorDefinition(numeric_code=1300, http_status=503, retry='after_backoff', message='index watermark is stale', remediation='wait for the required catalog watermark'),
    'INDEX_UNAVAILABLE': ErrorDefinition(numeric_code=1301, http_status=503, retry='after_backoff', message='required index is unavailable', remediation='restore the index or select an authorized fallback'),
    'CONSISTENCY_UNSATISFIED': ErrorDefinition(numeric_code=1302, http_status=503, retry='after_backoff', message='requested consistency cannot be satisfied', remediation='wait for the requested watermark'),
    'POLICY_DENIED': ErrorDefinition(numeric_code=1400, http_status=403, retry='after_reauthorization', message='current policy denied the operation', remediation='request authorized scope or purpose'),
    'PROCESSOR_DENIED': ErrorDefinition(numeric_code=1401, http_status=403, retry='after_reauthorization', message='processor constraints denied the operation', remediation='select an authorized processor'),
    'INSTRUCTION_AUTHORITY_DENIED': ErrorDefinition(numeric_code=1402, http_status=403, retry='never', message='instruction authority is insufficient', remediation='use an authorized instruction source'),
    'BUDGET_UNSATISFIABLE': ErrorDefinition(numeric_code=1500, http_status=422, retry='never', message='required context cannot fit the declared budget', remediation='increase the budget or narrow requirements'),
    'MISSING_REQUIRED_CONTEXT': ErrorDefinition(numeric_code=1501, http_status=404, retry='never', message='required authorized context is missing', remediation='ingest or authorize the required context'),
    'UNRESOLVED_CRITICAL_CONFLICT': ErrorDefinition(numeric_code=1502, http_status=409, retry='never', message='critical semantic conflict remains unresolved', remediation='resolve the conflict using typed evidence'),
    'DELTA_BASE_MISMATCH': ErrorDefinition(numeric_code=1600, http_status=400, retry='never', message='delta base does not match provider-present state', remediation='request a full bundle or matching delta'),
    'BUNDLE_INVALIDATED': ErrorDefinition(numeric_code=1601, http_status=409, retry='safe', message='bundle was invalidated', remediation='compile a new bundle from current inputs'),
    'REVISION_CONFLICT': ErrorDefinition(numeric_code=1700, http_status=409, retry='safe', message='expected revision does not match', remediation='reload current state and merge explicitly'),
    'HANDOFF_EXPIRED': ErrorDefinition(numeric_code=1701, http_status=409, retry='never', message='handoff has expired', remediation='create a new handoff capsule'),
    'HANDOFF_RECIPIENT_MISMATCH': ErrorDefinition(numeric_code=1702, http_status=403, retry='never', message='handoff recipient does not match', remediation='use a capsule addressed to the authenticated recipient'),
    'APPROVAL_REQUIRED': ErrorDefinition(numeric_code=1800, http_status=403, retry='after_reauthorization', message='effect requires approval', remediation='obtain approval bound to the exact intent'),
    'APPROVAL_STALE': ErrorDefinition(numeric_code=1801, http_status=403, retry='after_reauthorization', message='approval is stale', remediation='obtain a new approval for current semantics'),
    'EFFECT_UNKNOWN': ErrorDefinition(numeric_code=1802, http_status=409, retry='after_reconciliation', message='external effect outcome is unknown', remediation='reconcile before any retry'),
    'UNSAFE_RETRY': ErrorDefinition(numeric_code=1803, http_status=403, retry='after_reconciliation', message='effect cannot be safely retried', remediation='reconcile or resolve manually'),
    'REPLAY_INCOMPLETE': ErrorDefinition(numeric_code=1900, http_status=503, retry='never', message='replay evidence is incomplete', remediation='restore the listed dependencies'),
    'DEPENDENCY_UNAVAILABLE': ErrorDefinition(numeric_code=1901, http_status=404, retry='after_backoff', message='required dependency is unavailable', remediation='restore the named dependency'),
    'LIVE_AUTHORIZATION_REQUIRED': ErrorDefinition(numeric_code=1902, http_status=403, retry='after_reauthorization', message='live operation requires explicit authorization', remediation='authorize a new live execution'),
    'RATE_LIMITED': ErrorDefinition(numeric_code=2000, http_status=429, retry='after_backoff', message='request rate limit was reached', remediation='retry after the declared delay'),
    'DEADLINE_EXCEEDED': ErrorDefinition(numeric_code=2001, http_status=504, retry='safe', message='request deadline elapsed', remediation='retry only if the operation is safe'),
    'DEPENDENCY_DEGRADED': ErrorDefinition(numeric_code=2002, http_status=503, retry='after_backoff', message='dependency is degraded', remediation='retry after dependency recovery'),
    'INTERNAL': ErrorDefinition(numeric_code=2099, http_status=500, retry='never', message='internal service error', remediation='report the correlation identifier'),
}
