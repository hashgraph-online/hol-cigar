self-hosted-runner:
  labels:
    - refinement-development
    - refinement-promotion
    - refinement-shadow
    # GitHub-hosted label added after pinned actionlint 1.7.7's runner inventory.
    # This is a lint allowlist entry; release provenance still rejects self-hosted runners.
    - macos-15-intel

config-variables: null
