apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - migration-job.yaml
  - deployment.yaml
  - service.yaml
  - network-policy.yaml
images:
  - name: cigar-daemon
    newName: cigar-daemon
    newTag: wp18
