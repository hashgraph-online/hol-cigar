Implement the diagnosed change as one bounded unified diff. Preserve public APIs,
format invariants, deterministic behavior, and every forbidden path. Run only gates
named by the packet. Finish with a concise expected KPI effect and requested gates.
