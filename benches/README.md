# Benchmarks

[`cigarbench/`](cigarbench/) contains the versioned CIGARBench datasets, paired
randomization and execution protocol, strict raw-event schema, environment
capture, deterministic statistical comparator, report replay, and canary scan.

The checked-in smoke report proves harness reproducibility only. Its evidence is
explicitly ineligible for performance or outcome claims.
